version: 2
project_name: bulb-mcp

before:
  hooks:
    - go mod tidy

builds:
  - id: bulb-mcp
    main: ./cmd/bulb-mcp
    binary: bulb-mcp
    env: [CGO_ENABLED=0]
    goos: [linux, darwin]
    goarch: [amd64, arm64]

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: ["tar.gz"]

checksum:
  name_template: "checksums.txt"

changelog:
  use: github
  sort: asc
